apiVersion: v1
kind: ConfigMap
metadata:
  name: init-sql-config
  namespace: memo-app
  labels:
    app: memo
    component: db-init
  annotations:
    description: "초기 DB 스키마 생성 SQL"
data:
  init.sql: |
    CREATE DATABASE IF NOT EXISTS memo_app;

    CREATE USER IF NOT EXISTS 'memo'@'%' IDENTIFIED BY 'memomemomim';
    GRANT ALL PRIVILEGES ON memo_app.* TO 'memo'@'%';
    FLUSH PRIVILEGES;

    USE memo_app; 
